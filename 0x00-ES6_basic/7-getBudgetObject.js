export default function getBudgetObject(income, gdp, capita) {
  const budget = {
    income, // Shorthand property
    gdp, // Shorthand property
    capita, // Shorthand property
  };

  return budget;
}
